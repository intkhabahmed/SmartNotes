package com.intkhabahmed.smartnotes.utils;

public interface DateTimeListener {
    void selectedDate(String date);

    void selectedTime(String time);

    void dateTimeSelected(boolean isSelected);
}
